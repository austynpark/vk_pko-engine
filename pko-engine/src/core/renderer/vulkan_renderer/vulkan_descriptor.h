#ifndef VULKAN_DESCRIPTOR_H
#define VULKAN_DESCRIPTOR_H

// void vulkan_descriptor_set_allocate(RenderContext* context, DescriptorSet* set)



// First need descriptor set layout
// For the descriptor set layout, I need descriptor set binding
// binding number, descriptor count, type, shader_stages

// Layout createInfo
// pBindings
// bindingCount
// flags = 0

// DescriptorPool
// Allocate layouts
// Allocate

#endif  // VULKAN_DESCRIPTOR_H